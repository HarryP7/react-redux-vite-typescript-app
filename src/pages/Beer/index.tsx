import { LayoutApp } from 'core/components'
import { BeerDetailsContainer } from 'features/BeerDetails'

export const BeerDetailsPage = () => {
  return (
    <LayoutApp>
      <BeerDetailsContainer />
    </LayoutApp>
  )
}
