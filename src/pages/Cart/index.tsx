import { LayoutApp } from 'core/components'
import { ListCartContainer } from 'features/ListCart'

export const CartPage = () => {
  return (
    <LayoutApp>
      <ListCartContainer />
    </LayoutApp>
  )
}
