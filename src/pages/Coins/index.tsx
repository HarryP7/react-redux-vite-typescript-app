import { LayoutApp } from 'core/components'
import { CoinsContainer } from 'features/Coins'

export const CoinsPage = () => {
  return (
    <LayoutApp>
      <CoinsContainer />
    </LayoutApp>
  )
}
